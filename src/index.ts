export const создатьКартофель = (имяКартошки: string, количествоКартошек: number) => (console.log(`вы создали ${количествоКартошек} картошку/шки/шек`), { картошки: Array.from({ length: количествоКартошек }, (_, i) => ({ имя: `${имяКартошки}${i}` })) } as const)